name: Spellcheck Workflow
on:
  push:
    branches: [main]
    paths:
      - '**/*.yml'
      - '!.github/**/*.yml'
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Spellcheck init
    runs-on: ubuntu-latest
    steps:
    # The checkout step
    - uses: actions/checkout@v6
    - uses: rojopolis/spellcheck-github-actions@v0
      name: Spellcheck - YAML
      with:
        source_files: resume-en.yml
        task_name: YAML
  # check:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v6

  #     - name: Spellcheck
  #       uses: igsekor/pyspelling-any@v1
  #       # with:
  #       #   configFile: '.my-custom-config.yml'
